///
/// OnDeviceMode.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrospeech {

  /**
   * An enum which can be represented as a JavaScript union (OnDeviceMode).
   */
  enum class OnDeviceMode {
    PREFER      SWIFT_NAME(prefer) = 0,
    REQUIRE      SWIFT_NAME(require) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrospeech

namespace margelo::nitro {

  // C++ OnDeviceMode <> JS OnDeviceMode (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrospeech::OnDeviceMode> final {
    static inline margelo::nitro::nitrospeech::OnDeviceMode fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("prefer"): return margelo::nitro::nitrospeech::OnDeviceMode::PREFER;
        case hashString("require"): return margelo::nitro::nitrospeech::OnDeviceMode::REQUIRE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum OnDeviceMode - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrospeech::OnDeviceMode arg) {
      switch (arg) {
        case margelo::nitro::nitrospeech::OnDeviceMode::PREFER: return JSIConverter<std::string>::toJSI(runtime, "prefer");
        case margelo::nitro::nitrospeech::OnDeviceMode::REQUIRE: return JSIConverter<std::string>::toJSI(runtime, "require");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert OnDeviceMode to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("prefer"):
        case hashString("require"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
