///
/// SpeechRecognitionError.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrospeech {

  /**
   * An enum which can be represented as a JavaScript enum (SpeechRecognitionError).
   */
  enum class SpeechRecognitionError {
    UNKNOWN      SWIFT_NAME(unknown) = 0,
    LOCALENOTSUPPORTED      SWIFT_NAME(localenotsupported) = 1,
    RECOGNITIONTASKFAILED      SWIFT_NAME(recognitiontaskfailed) = 2,
    IOSSPEECHPERMISSIONNOTDETERMINED      SWIFT_NAME(iosspeechpermissionnotdetermined) = 3,
    SESSIONSTARTFAILED      SWIFT_NAME(sessionstartfailed) = 4,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrospeech

namespace margelo::nitro {

  // C++ SpeechRecognitionError <> JS SpeechRecognitionError (enum)
  template <>
  struct JSIConverter<margelo::nitro::nitrospeech::SpeechRecognitionError> final {
    static inline margelo::nitro::nitrospeech::SpeechRecognitionError fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<margelo::nitro::nitrospeech::SpeechRecognitionError>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrospeech::SpeechRecognitionError arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double number = value.getNumber();
      int integer = static_cast<int>(number);
      if (number != integer) {
        // The integer is not the same value as the double - we truncated floating points.
        // Enums are all integers, so the input floating point number is obviously invalid.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 4;
    }
  };

} // namespace margelo::nitro
