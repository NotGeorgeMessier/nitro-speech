///
/// HybridTTSSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#include "HybridTTSSpec.hpp"

// Forward declaration of `HybridTTSSpec_cxx` to properly resolve imports.
namespace NitroSpeech { class HybridTTSSpec_cxx; }

// Forward declaration of `TTSParams` to properly resolve imports.
namespace margelo::nitro::nitrospeech { struct TTSParams; }

#include <string>
#include "TTSParams.hpp"
#include <optional>
#include <NitroModules/Promise.hpp>

#include "NitroSpeech-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::nitrospeech {

  /**
   * The C++ part of HybridTTSSpec_cxx.swift.
   *
   * HybridTTSSpecSwift (C++) accesses HybridTTSSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridTTSSpec_cxx can directly inherit from the C++ class HybridTTSSpec
   * to simplify the whole structure and memory management.
   */
  class HybridTTSSpecSwift: public virtual HybridTTSSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridTTSSpecSwift(const NitroSpeech::HybridTTSSpec_cxx& swiftPart):
      HybridObject(HybridTTSSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline NitroSpeech::HybridTTSSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline double add(double a, double b) override {
      auto __result = _swiftPart.add(std::forward<decltype(a)>(a), std::forward<decltype(b)>(b));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline void speak(const std::string& text, const TTSParams& params) override {
      auto __result = _swiftPart.speak(text, std::forward<decltype(params)>(params));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline std::shared_ptr<Promise<bool>> isSpeaking() override {
      auto __result = _swiftPart.isSpeaking();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline void stop() override {
      auto __result = _swiftPart.stop();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline std::shared_ptr<Promise<bool>> pause() override {
      auto __result = _swiftPart.pause();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<bool>> resume() override {
      auto __result = _swiftPart.resume();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    NitroSpeech::HybridTTSSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::nitrospeech
